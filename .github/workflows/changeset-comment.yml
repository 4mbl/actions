on:
  workflow_call:
    inputs:
      os:
        required: false
        type: string
        default: ubuntu-slim
      timeout-minutes:
        required: false
        type: number
        default: 10
        description: Timeout in minutes for each job.

concurrency:
  group: ${{ github.workflow_ref }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  comment:
    if: github.actor != 'dependabot[bot]'

    runs-on: ${{ inputs.os }}
    timeout-minutes: ${{ inputs.timeout-minutes }}

    permissions:
      pull-requests: write

    steps:
      - name: Post comment
        uses: 4mbl/actions/changeset/pr-comment@7c3d479d93d92e1e04adc5644cc219cb5772f65f # v1
